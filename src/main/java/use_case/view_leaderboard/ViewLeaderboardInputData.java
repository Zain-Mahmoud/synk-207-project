package use_case.view_leaderboard;

/**
 * Input Data for the View Leaderboard Use Case.
 * Currently no input is required, but this class exists for consistency
 * and future extensibility (e.g., filtering options).
 */
public class ViewLeaderboardInputData {
    /**
     * Default constructor for ViewLeaderboardInputData.
     * Currently no input parameters are needed for viewing the leaderboard.
     */
    public ViewLeaderboardInputData() {
        // No initialization needed - this class exists for consistency
        // and future extensibility (e.g., filtering options).
    }
}

